{
  "name": "@longgu/cli",
  "version": "1.0.0",
  "private": true,
  "license": "PolyForm-Noncommercial-1.0.0",
  "type": "module",
  "bin": {
    "longgu": "./dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "verify": "npm run typecheck && npm run build && npm test && openspec validate --all",
    "cli": "node dist/cli/index.js",
    "dev:cli": "tsx src/cli/index.ts"
  },
  "dependencies": {
    "commander": "^14.0.1",
    "diff": "^9.0.0",
    "yaml": "^2.8.1",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.14"
  }
}
